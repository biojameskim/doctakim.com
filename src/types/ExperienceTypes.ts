export type EmploymentCardType = {
  company: string,
  website: string,
  role: string,
  image: string,
  alt: string,
  date: string,
  description: string
};

export type CourseworkCardType = {
  courseTitle: string,
  courseNumber: string,
  affiliation?: string
}